#Install the following PIPs

pip install llama-index-llms-anthropic -q
pip install llama-index -q

#Get API key from Claude
#Get API key from https://site.financialmodelingprep.com/developer/ (you can sign in with Google to get API Key - API Key under Dashboard)
 
#export the env variables needed for the python script
CLAUDE_API_KEY 
FINANCIAL_MODELING_PREP_API_KEY 
